# ROADMAP — Zface / Что осталось сделать

Обновляется Клодом автоматически — но перед добавлением нового пункта Клод **спрашивает**.
Всё уже реализованное — в **[DONE.md](DONE.md)**.

🔴 блокер релиза  🟡 важно, не срочно  🟢 опционально

---

## 🔴 Блокеры — без этого в стор не попасть

- [ ] **Название приложения** — решить окончательно. Вариант «Ной» обсуждался — метафора: и имя, и «ноет». От названия зависит всё ниже.

- [ ] **Иконка и сплэш** — заменить дефолтные Expo-заглушки на финальный дизайн.

  | Файл | Размер | Примечания |
  |------|--------|-----------|
  | `assets/icon.png` | 1024×1024 PNG | Без прозрачности — iOS не принимает |
  | `assets/adaptive-icon.png` | 1024×1024 PNG | Android, иконка в центральных 66% |
  | `assets/splash-icon.png` | ~512×512 PNG | С прозрачностью, `resizeMode: "contain"` |

- [ ] **Firebase FCM для Android** — без этого push-уведомления на Android не работают в production. Бесплатно, нужно до сборки production-билда.

  1. Создать проект на https://console.firebase.google.com
  2. Добавить Android-приложение (package name из `app.json`)
  3. Скачать `google-services.json`, положить в корень проекта
  4. В `app.json` добавить `"android": { "googleServicesFile": "./google-services.json" }`
  5. `eas credentials` → загрузить FCM credentials

- [ ] **Скрыть личные данные в политике конфиденциальности** — создать GitHub Organization с именем приложения, перенести репо, обновить URL политики. Заменить личный email в `docs/index.html` на отдельный ящик для приложения (корпоративный, не личный).

- [ ] **Возрастная категория 17+** — в метадате App Store (Rating → 17+) и Google Play (Content Rating → Mature 17+). Контент о депрессии/суициде требует этой отметки иначе App Review отклонит.

- [ ] **ASO-метадата** — заполнить до подачи:
  - Название (30 симв): `[Бренд] — поддержка рядом`
  - Подзаголовок (30 симв): `для тех кому сейчас тяжело`
  - Ключевые слова (100 симв): `тревога,стресс,депрессия,поддержка,психология,настроение,чат,помощь,одиночество`
  - 5 скриншотов: уровень / комнаты / активности / письма / «анонимно без осуждения»

- [ ] **Deep link для подтверждения email** — `EMAIL_CONFIRM_ENABLED` в `config.js` сейчас `false`. Перед релизом: включить в Supabase Dashboard + поставить `true` в коде + настроить deep link, чтобы письмо открывало приложение, а не браузер.

- [x] **Кризис-фильтр в письмах** — в `LetterScreen` добавлен баннер с телефоном доверия при наборе кризисных фраз, как в чатах.

- [x] **Жалоба на конкретное сообщение** — кнопка «Пожаловаться» в `ChatActionMenu` для чужих сообщений. Поля `message_id`, `message_text`, `message_table` добавлены в таблицу `reports`.

- [ ] **Email-алерт разработчику при новой жалобе** — жалобы сейчас падают в таблицу и там лежат. Настроить вручную в Supabase Dashboard → Database → Webhooks: триггер на INSERT в `reports` → POST на edge function `report-alert` (функция задеплоена, нужен `RESEND_API_KEY` в Supabase Secrets).

---

## 🟡 Удержание и вовлечение

- [x] **Быстрый чекин настроения** — слайдер 1–10 на главном экране, 1 раз в день. После тапа показывает сколько человек сегодня чувствуют себя так же. Таблица `mood_checkins`.

- [x] **Ночная комната** — появляется в MessagesScreen и лобби комнат с 23:00 до 06:00. Для тех кому не спится. Таблица `messages` с `level = 'night'`, доступна всем уровням.

- [x] **Контекстные пуши** — ночной пуш (23:00, ночная комната), пятничный пуш (21:00), напоминание о возврате (через 3 дня без открытия — откладывается при каждом запуске).

- [x] **Мягкое отражение присутствия** — карточка в ProfileScreen: «ты здесь уже N дней», «поддержал N человек», «N ответов на вопрос дня». Считается из существующих данных.

- [x] **Анонимное письмо** — экран `LetterScreen`: пишешь письмо в никуда, случайный человек с тем же уровнем получает его завтра. Входящие письма с уведомлением о новом. Таблица `anonymous_letters`. Модуль в HomeScreen.

- [x] **«Похожий на тебя»** — карточка на главном экране (раз в 7 дней): алгоритм находит пользователя с пересечением слов в ответах на вопрос дня. «Кажется, вы похожи» → профиль или «не сейчас». Таблица `similar_user_shown`.

- [x] **Пуш «письмо дошло»** — когда адресат открывает анонимное письмо, отправителю приходит тихий пуш: «твоё письмо дошло». Без имени. Закрывает петлю — человек узнаёт что написал не в пустоту.

- [x] **Пуш «кто-то сказал что ты помог»** — когда кто-то нажимает «Он мне помог» на профиле пользователя, тому приходит пуш: «кто-то написал, что ты им помог». Триггер: INSERT в `user_helps` → push к `helped_id`.

- [x] **Пуш при комментарии к посту** — когда кто-то комментирует твой пост в ленте, автору приходит пуш. INSERT в `post_comments` → push к `author_id` поста.

- [x] **Недельный пуш-отчёт** — каждый понедельник 19:00 один пуш: «эта неделя: X дней подряд · N писем · помог M людям · K ответов». Данные из `login_streak`, `anonymous_letters`, `user_helps`, `daily_answers`.

- [x] **Пуш «сегодня в комнате говорили о...»** — каждое утро в 10:00 пуш с топ-3 словами из комнаты пользователя за последние 24ч. Слова без прямых цитат. Пересчитывается при каждом открытии приложения.

- [x] **Онбординг первых 3 дней** — мягкие подсказки на первый вход: в комнатах «здесь можно просто сидеть молча», на рыбалке «здесь не надо ничего объяснять». AsyncStorage, исчезают через 5 сек или по тапу.

- [x] **«Фокус дня»** — приоритетная карточка под приветствием на HomeScreen. Приоритет: письмо ждёт → вопрос дня → чекин. Скрывается когда всё выполнено.

- [x] **«Ты сделал всё на сегодня»** — момент завершённости когда выполнены все дневные действия (вопрос дня + чекин + слово дня). Текст: «на сегодня всё. Ты был здесь — это уже что-то».

---

## 🟡 Техдолг и безопасность

- [ ] **Crash reporting** — подключить Sentry или аналог. Без этого о падениях у пользователей узнаём от самих пользователей. Час настройки.

- [ ] **Базовая аналитика событий** — какие экраны открывают, где уходят, что не находят. Amplitude или простой Supabase-лог событий. Без аналитики growth — гадание.

- [ ] **Рефакторинг аватаров → Supabase Storage** — сейчас base64 в поле `users.avatar_url`. При тысяче пользователей таблица `users` начнёт тормозить на любом SELECT. Перенести в Storage bucket как медиа постов.

---

## 🟢 Опциональное

- [x] **Коллекция рыб** — таблица `caught_fish`, прогресс-бар + сетка в ProfileScreen, «поймано X из 11». Скрыты до поимки.

- [x] **Скрытые ачивки** — часть ачивок показывается как «🔮 ???» до получения. Только активностные — не связанные с уровнем/состоянием.
