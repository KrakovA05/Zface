---
phase: 07-release-prep
plan: "08"
type: execute
wave: 1
depends_on: []
files_modified:
  - ustal/screens/DirectMessageScreen.js
  - ustal/screens/ThoughtsScreen.js
  - ustal/screens/FeedScreen.js
  - ustal/screens/NotificationsScreen.js
autonomous: true
requirements:
  - RELEASE-BUGS
must_haves:
  truths:
    - "Реакции на сообщения в DM отображаются и сохраняются корректно"
    - "Реакции на анонимные мысли сохраняются и отображаются корректно"
    - "toggleLike в FeedScreen не выполняется если store.userId=null"
    - "NotificationsScreen переходит на UserProfile с корректным параметром user (объект)"
    - "ThoughtsScreen submit проверяет store.userId перед вставкой"
  artifacts:
    - path: "ustal/screens/DirectMessageScreen.js"
      provides: "исправленные реакции (reaction вместо emoji)"
      contains: "reaction:"
    - path: "ustal/screens/ThoughtsScreen.js"
      provides: "исправленные реакции (reaction вместо reaction_type)"
      contains: ".reaction"
    - path: "ustal/screens/FeedScreen.js"
      provides: "null guard в toggleLike"
      contains: "if (!store.userId) return"
    - path: "ustal/screens/NotificationsScreen.js"
      provides: "корректные params при навигации на UserProfile"
      contains: "user: { user_id"
  key_links:
    - from: "message_reactions.reaction (DB)"
      to: "DirectMessageScreen.js"
      via: "SELECT/INSERT/upsert поле"
      pattern: "reaction:"
    - from: "thought_reactions.reaction (DB)"
      to: "ThoughtsScreen.js"
      via: "SELECT/upsert поле"
      pattern: "\\.reaction"
---

<objective>
Исправить 5 критических багов из code review (CR-01..CR-05) которые ломают ключевые фичи.

Purpose: Все 5 багов — production-blockers: реакции в DM не работают совсем, реакции на мысли не работают, like в ленте может записать null user_id, переход на профиль из уведомлений крашится, submit мысли может записать null user_id.
Output: Исправленные DirectMessageScreen.js, ThoughtsScreen.js, FeedScreen.js, NotificationsScreen.js.
</objective>

<execution_context>
@/Users/user/Zface/.planning/phases/phase-07-release-prep/07-REVIEW.md
</execution_context>

<context>
@/Users/user/Zface/.planning/phases/phase-07-release-prep/07-REVIEW.md
@/Users/user/Zface/CLAUDE.md

<interfaces>
DB schema (из CLAUDE.md):
```
message_reactions: (id, message_id UUID, message_table TEXT, user_id UUID, reaction TEXT, created_at)
  unique index: (message_id, message_table, user_id)

thought_reactions: (id, thought_id UUID, user_id UUID, reaction TEXT, created_at)

UserProfileScreen ожидает: route.params.user — объект с полями user_id, username, level, avatar_url, status
```

CR-01 (DirectMessageScreen — поле emoji → reaction):
```js
// БЫЛО:
.select('message_id, emoji, user_id')
const r = { ..., emoji };
g[r.emoji]

// НАДО:
.select('message_id, reaction, user_id')
const r = { ..., reaction: emoji };
g[r.reaction]
```

CR-03 (FeedScreen — toggleLike null guard):
```js
// ДОБАВИТЬ первой строкой toggleLike:
if (!store.userId) return;
```

CR-04 (NotificationsScreen — params для UserProfile):
```js
// БЫЛО:
navigation.navigate('UserProfile', { userId: notif.data.user_id });

// НАДО:
navigation.navigate('UserProfile', {
  user: { user_id: notif.data.user_id, username: '', level: 'green', avatar_url: null, status: '' }
});
```

CR-05 (ThoughtsScreen — поле reaction_type → reaction):
```js
// БЫЛО:
.select('reaction_type')
reaction_type: type
r.reaction_type

// НАДО:
.select('reaction')
reaction: type
r.reaction
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Исправить CR-01 и CR-05 — поля реакций (emoji/reaction_type → reaction)</name>
  <files>ustal/screens/DirectMessageScreen.js, ustal/screens/ThoughtsScreen.js</files>
  <read_first>
    - /Users/user/Zface/ustal/screens/DirectMessageScreen.js (прочитать весь файл, строки 100-170)
    - /Users/user/Zface/ustal/screens/ThoughtsScreen.js (прочитать весь файл)
    - /Users/user/Zface/.planning/phases/phase-07-release-prep/07-REVIEW.md (CR-01 строки 39-71, CR-05 строки 139-157)
  </read_first>
  <behavior>
    - После исправления: SELECT из message_reactions возвращает поле reaction (не emoji)
    - После исправления: groupReactions() группирует по r.reaction (не r.emoji)
    - После исправления: upsert в message_reactions использует ключ reaction (не emoji)
    - После исправления: SELECT из thought_reactions возвращает поле reaction (не reaction_type)
    - После исправления: upsert в thought_reactions использует ключ reaction (не reaction_type)
    - После исправления: grouping в ThoughtsScreen использует r.reaction (не r.reaction_type)
  </behavior>
  <action>
    В DirectMessageScreen.js найти ВСЕ вхождения поля emoji в контексте запросов к message_reactions и переименовать в reaction.

    Конкретно (grep покажет строки):
    1. В SELECT запросе: `.select('message_id, emoji, user_id')` → `.select('message_id, reaction, user_id')`
    2. В groupReactions(): `g[r.emoji]` → `g[r.reaction]` (все вхождения)
    3. В toggleReaction(): объект `{ ..., emoji }` → `{ ..., reaction: emoji }` (ключ объекта, значение переменной остаётся)
       Переменная `emoji` в функции — это локальный параметр (строка типа '👍'), оставить без изменений.
       Только КЛЮЧ объекта при вставке меняется с `emoji` на `reaction`.

    В ThoughtsScreen.js найти ВСЕ вхождения reaction_type и заменить на reaction.

    Конкретно:
    1. В SELECT: `.select('reaction_type')` → `.select('reaction')`
    2. В upsert: `reaction_type: type` → `reaction: type`
    3. В группировке: `r.reaction_type` → `r.reaction` (все вхождения)
    4. Если есть проверки типа `g[r.reaction_type] !== undefined` → `g[r.reaction] !== undefined`

    Использовать grep для нахождения всех вхождений перед изменением:
    ```bash
    grep -n "emoji\|reaction_type" /Users/user/Zface/ustal/screens/DirectMessageScreen.js
    grep -n "reaction_type" /Users/user/Zface/ustal/screens/ThoughtsScreen.js
    ```
  </action>
  <verify>
    <automated>
grep -c "\.select.*emoji" /Users/user/Zface/ustal/screens/DirectMessageScreen.js && echo "FAIL: emoji in select" || echo "OK: no emoji in select"
grep -c "reaction_type" /Users/user/Zface/ustal/screens/ThoughtsScreen.js && echo "FAIL: reaction_type remains" || echo "OK: no reaction_type"
cd /Users/user/Zface/ustal && npm test -- --passWithNoTests 2>&1 | tail -3
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "\.select.*emoji" DirectMessageScreen.js → 0 (поле emoji убрано из SELECT)
    - grep "'emoji'" DirectMessageScreen.js в контексте message_reactions → 0 совпадений как ключ объекта
    - grep "reaction_type" ThoughtsScreen.js → 0 совпадений
    - DirectMessageScreen.js содержит ".select('message_id, reaction, user_id')"
    - ThoughtsScreen.js содержит "reaction: type" (не reaction_type)
    - npm test проходит без ошибок
    Команды:
    ```bash
    grep -n "emoji\|reaction_type" /Users/user/Zface/ustal/screens/DirectMessageScreen.js
    grep -n "reaction_type" /Users/user/Zface/ustal/screens/ThoughtsScreen.js
    ```
    Оба должны вернуть 0 строк.
  </acceptance_criteria>
  <done>CR-01 и CR-05 исправлены — реакции в DM и мыслях теперь используют правильное поле DB.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Исправить CR-03, CR-04, WR-08 — null guards и навигация</name>
  <files>ustal/screens/FeedScreen.js, ustal/screens/NotificationsScreen.js</files>
  <read_first>
    - /Users/user/Zface/ustal/screens/FeedScreen.js (строки 185-210 — функция toggleLike)
    - /Users/user/Zface/ustal/screens/NotificationsScreen.js (строки 75-100 — handleTap, навигация)
    - /Users/user/Zface/ustal/screens/ThoughtsScreen.js (строки 95-120 — функция submit)
    - /Users/user/Zface/.planning/phases/phase-07-release-prep/07-REVIEW.md (CR-03 строки 86-113, CR-04 строки 117-135, WR-08 строки 287-299)
  </read_first>
  <behavior>
    - toggleLike с store.userId=null → return немедленно, ни DB запроса, ни optimistic update
    - handleTap с notif.type='friend_request' → navigate('UserProfile', { user: { user_id: ..., username: '', level: 'green', avatar_url: null, status: '' } })
    - submit() в ThoughtsScreen с store.userId=null → return немедленно
  </behavior>
  <action>
    CR-03 — FeedScreen.js, функция toggleLike:
    Добавить первой строкой тела функции (перед optimistic update):
    ```js
    if (!store.userId) return;
    ```
    Найти функцию toggleLike через: `grep -n "toggleLike" /Users/user/Zface/ustal/screens/FeedScreen.js`

    CR-04 — NotificationsScreen.js, функция handleTap (или аналогичная):
    Найти строку с `navigation.navigate('UserProfile', { userId:` и заменить на:
    ```js
    navigation.navigate('UserProfile', {
      user: {
        user_id: notif.data.user_id,
        username: '',
        level: 'green',
        avatar_url: null,
        status: '',
      }
    });
    ```
    Найти через: `grep -n "UserProfile\|userId" /Users/user/Zface/ustal/screens/NotificationsScreen.js`

    WR-08 — ThoughtsScreen.js, функция submit (если не исправлена в Task 1):
    Добавить первой строкой тела функции submit:
    ```js
    if (!store.userId) return;
    ```
    Найти через: `grep -n "const submit\|submit = " /Users/user/Zface/ustal/screens/ThoughtsScreen.js`
  </action>
  <verify>
    <automated>
grep -c "if (!store.userId) return" /Users/user/Zface/ustal/screens/FeedScreen.js
grep -n "UserProfile" /Users/user/Zface/ustal/screens/NotificationsScreen.js | grep -c "user_id:"
grep -c "if (!store.userId) return" /Users/user/Zface/ustal/screens/ThoughtsScreen.js
cd /Users/user/Zface/ustal && npm test -- --passWithNoTests 2>&1 | tail -3
    </automated>
  </verify>
  <acceptance_criteria>
    - FeedScreen.js содержит "if (!store.userId) return" в теле toggleLike
    - NotificationsScreen.js содержит "user: {" рядом со строкой navigate UserProfile (не { userId: })
    - NotificationsScreen.js НЕ содержит "{ userId: notif" при навигации на UserProfile
    - ThoughtsScreen.js содержит "if (!store.userId) return" в теле submit
    - npm test проходит
    Команды:
    ```bash
    grep -A2 "toggleLike" /Users/user/Zface/ustal/screens/FeedScreen.js | head -6
    grep -B1 -A5 "UserProfile" /Users/user/Zface/ustal/screens/NotificationsScreen.js
    ```
  </acceptance_criteria>
  <done>CR-03, CR-04, WR-08 исправлены — null guards добавлены, навигация на UserProfile использует корректную структуру params.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store.userId → DB write | Null userId может создать строки с null в защищённых полях |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08-01 | Tampering | post_likes с null user_id | mitigate | if (!store.userId) return в toggleLike — исправлено в этом плане |
| T-07-08-02 | Tampering | anonymous_thoughts с null user_id | mitigate | if (!store.userId) return в submit — исправлено в этом плане |
| T-07-08-03 | Information Disclosure | message_reactions с неверным полем | mitigate | Переименование emoji → reaction — исправлено. Данные теперь сохраняются корректно. |
</threat_model>

<verification>
```bash
# CR-01: нет emoji в SELECT к message_reactions
grep -n "emoji" /Users/user/Zface/ustal/screens/DirectMessageScreen.js | grep -v "// \|local var"

# CR-05: нет reaction_type
grep -n "reaction_type" /Users/user/Zface/ustal/screens/ThoughtsScreen.js

# CR-03: null guard в toggleLike
grep -A3 "const toggleLike" /Users/user/Zface/ustal/screens/FeedScreen.js | head -5

# CR-04: UserProfile params
grep -A6 "navigate.*UserProfile" /Users/user/Zface/ustal/screens/NotificationsScreen.js

# Тесты
cd /Users/user/Zface/ustal && npm test -- --passWithNoTests
```
</verification>

<success_criteria>
- CR-01: DirectMessageScreen.js использует поле "reaction" (не "emoji") в запросах к message_reactions
- CR-03: FeedScreen.toggleLike начинается с "if (!store.userId) return"
- CR-04: NotificationsScreen передаёт user: { user_id: ... } в UserProfile (не { userId: ... })
- CR-05: ThoughtsScreen.js не содержит "reaction_type"
- WR-08: ThoughtsScreen.submit начинается с "if (!store.userId) return"
- npm test зелёный
</success_criteria>

<output>
После завершения создать .planning/phases/phase-07-release-prep/07-08-SUMMARY.md с:
- списком исправленных багов (CR-01, CR-03, CR-04, CR-05, WR-08)
- конкретными изменёнными строками
</output>
