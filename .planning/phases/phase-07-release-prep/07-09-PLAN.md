---
phase: 07-release-prep
plan: "09"
type: execute
wave: 1
depends_on: []
files_modified:
  - ustal/theme.js
  - ustal/screens/FishingScreen.js
  - ustal/screens/SupportScreen.js
  - ustal/screens/BreathingScreen.js
autonomous: true
requirements:
  - RELEASE-UI
must_haves:
  truths:
    - "theme.js colors.accent = '#8B7355' (тёплый коричневый, не #7c3aed фиолетовый)"
    - "theme.js colors.pink = '#E07060' (не #c0392b)"
    - "FishingScreen не рендерит emoji как текст — использует Ionicons или букву рыбы"
    - "SupportScreen использует useSafeAreaInsets вместо paddingTop: 56"
    - "BreathingScreen использует useSafeAreaInsets.top для header"
  artifacts:
    - path: "ustal/theme.js"
      provides: "корректные цветовые токены"
      contains: "#8B7355"
    - path: "ustal/screens/FishingScreen.js"
      provides: "экран без emoji в UI"
      contains: "Ionicons"
  key_links:
    - from: "theme.js colors.accent"
      to: "все экраны (203 использования)"
      via: "import { colors } from '../theme'"
      pattern: "colors.accent"
---

<objective>
Исправить критические UI-проблемы из code review — цветовой контракт дизайн-системы и emoji в FishingScreen.

Purpose: theme.js с accent=#7c3aed (фиолетовый) ломает весь дизайн приложения — все 203 кнопки, иконки, бейджи рендерятся в фиолетовом вместо тёплого коричневого. FishingScreen с emoji-символами в 64px нарушает правило "Ionicons везде, emoji не используются".
Output: theme.js с корректными цветами, FishingScreen без emoji в UI, SupportScreen и BreathingScreen с корректными safe-area insets.
</objective>

<execution_context>
@/Users/user/Zface/.planning/phases/phase-07-release-prep/07-UI-REVIEW.md
</execution_context>

<context>
@/Users/user/Zface/.planning/phases/phase-07-release-prep/07-UI-REVIEW.md
@/Users/user/Zface/CLAUDE.md

<interfaces>
Текущий theme.js (частично):
```js
accent: '#7c3aed',   // НЕВЕРНО — должно быть '#8B7355'
pink: '#c0392b',     // НЕВЕРНО — должно быть '#E07060'
```

Дизайн-контракт (из CLAUDE.md):
- colors.accent = '#8B7355' (тёплый коричневый)
- Ionicons везде, эмодзи не используются

FishingScreen emoji (из 07-UI-REVIEW.md):
- Строка ~435: `<Text style={styles.resultEmoji}>{result.emoji}</Text>` размером 64px (legendary: 80px)
- Строка ~473: `<Text>{c.emoji}</Text>` в коллекции
- Строки 57-61: SCENE.icon = emoji ('🌅', '☀️', '🌆', '🌙') в timeBadge

Замена emoji на Ionicons по смыслу:
- '🌅' (рассвет) → 'sunny-outline'
- '☀️' (день) → 'sunny'
- '🌆' (вечер) → 'partly-sunny-outline'
- '🌙' (ночь) → 'moon-outline'
- Рыба (result.emoji) → 'fish-outline' (все рыбы одинаково) + цвет по rarity
- Легендарная → 'star-outline'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Исправить theme.js — accent и pink цвета</name>
  <files>ustal/theme.js</files>
  <read_first>
    - /Users/user/Zface/ustal/theme.js (прочитать весь файл перед изменением)
    - /Users/user/Zface/.planning/phases/phase-07-release-prep/07-UI-REVIEW.md (Pillar 3: Color, строки 84-107)
  </read_first>
  <action>
    В ustal/theme.js найти и заменить ДВА значения:

    1. accent: '#7c3aed' → '#8B7355'
    2. pink: '#c0392b' → '#E07060'

    Остальные цвета не трогать.

    После исправления — верифицировать grep-ом что '#7c3aed' больше нет в theme.js.
    Также проверить нет ли хардкода '#7c3aed' в других файлах (ChatScreen.js, DirectMessageScreen.js):
    ```bash
    grep -rn "#7c3aed" /Users/user/Zface/ustal/screens/
    ```
    Если хардкод найден — заменить на colors.accent.

    ВАЖНО: Из 07-UI-REVIEW.md Warning: хардкод #7c3aed есть в:
    - DirectMessageScreen.js строки 300, 302, 437, 441 (crisis banner)
    - ChatScreen.js строки 256, 258, 343, 347
    Заменить все `#7c3aed` → `colors.accent` в этих файлах (убедиться что colors импортирован).
  </action>
  <verify>
    <automated>grep -rn "#7c3aed" /Users/user/Zface/ustal/ 2>/dev/null | grep -v node_modules | grep -v ".expo" && echo "FAIL: hardcoded purple remains" || echo "OK: no #7c3aed"</automated>
  </verify>
  <acceptance_criteria>
    - theme.js содержит "accent: '#8B7355'" (не #7c3aed)
    - theme.js содержит "pink: '#E07060'" (не #c0392b)
    - grep -rn "#7c3aed" ustal/ (кроме node_modules) → 0 результатов
    Команды:
    ```bash
    grep "accent:" /Users/user/Zface/ustal/theme.js
    grep "pink:" /Users/user/Zface/ustal/theme.js
    grep -rn "#7c3aed" /Users/user/Zface/ustal/ --include="*.js" | grep -v node_modules
    ```
    Ожидаемый вывод: accent: '#8B7355', pink: '#E07060', нет '#7c3aed'
  </acceptance_criteria>
  <done>theme.js исправлен — весь дизайн приложения теперь использует тёплый коричневый вместо фиолетового.</done>
</task>

<task type="auto">
  <name>Task 2: Убрать emoji из FishingScreen и исправить safe-area в SupportScreen/BreathingScreen</name>
  <files>ustal/screens/FishingScreen.js, ustal/screens/SupportScreen.js, ustal/screens/BreathingScreen.js</files>
  <read_first>
    - /Users/user/Zface/ustal/screens/FishingScreen.js (прочитать целиком — найти все emoji в JSX)
    - /Users/user/Zface/ustal/screens/SupportScreen.js (найти строки с paddingTop: 56 и header style)
    - /Users/user/Zface/ustal/screens/BreathingScreen.js (найти header style, строка ~94)
    - /Users/user/Zface/.planning/phases/phase-07-release-prep/07-UI-REVIEW.md (Pillar 2: строки 61-79, Pillar 5: строки 128-132)
  </read_first>
  <action>
    **FishingScreen.js — убрать emoji:**

    1. Найти SCENE объект (константа с иконками рассвета/дня/вечера/ночи):
       grep -n "🌅\|☀️\|🌆\|🌙\|SCENE\|icon.*emoji" FishingScreen.js
       Заменить emoji-значения icon на имена Ionicons:
       - '🌅' → 'sunny-outline'
       - '☀️' → 'sunny'
       - '🌆' → 'partly-sunny-outline'
       - '🌙' → 'moon-outline'
       В JSX где рендерится sc.icon как `<Text>{sc.icon}</Text>`:
       заменить на `<Ionicons name={sc.icon} size={14} color={colors.muted} />`

    2. Найти result.emoji в resultCard JSX (~строка 435):
       `<Text style={styles.resultEmoji}>{result.emoji}</Text>` или аналог
       Заменить на:
       `<Ionicons name="fish-outline" size={48} color={rarityColor || colors.accent} />`
       где rarityColor — цвет из RARITY[result.rarity] или константы.
       Для легендарных (legendary): `name="star-outline"` size={56}.
       Удалить styles.resultEmoji если он только для emoji.

    3. Найти c.emoji в коллекции (~строка 473):
       `<Text>{c.emoji}</Text>` → `<Ionicons name="fish-outline" size={16} color={colors.accent} />`

    4. Если FISH массив содержит поле emoji — оставить как есть (данные), просто не рендерить.

    **SupportScreen.js — safe-area:**
    Найти `paddingTop: 56` в header style.
    Добавить в компонент: `const insets = useSafeAreaInsets();`
    Если useSafeAreaInsets не импортирован: `import { useSafeAreaInsets } from 'react-native-safe-area-context';`
    Заменить `paddingTop: 56` на `paddingTop: insets.top + 12`

    **BreathingScreen.js — safe-area:**
    Найти safeArea style (строка ~94) и header style.
    Добавить: `const insets = useSafeAreaInsets();`
    В header style или safeArea добавить: `paddingTop: insets.top`
    Если useSafeAreaInsets уже импортирован — только добавить использование.
  </action>
  <verify>
    <automated>
grep -c "🌅\|☀️\|🌆\|🌙" /Users/user/Zface/ustal/screens/FishingScreen.js && echo "FAIL: scene emoji remains" || echo "OK"
grep -c "result\.emoji\|c\.emoji" /Users/user/Zface/ustal/screens/FishingScreen.js && echo "FAIL: result emoji remains" || echo "OK"
grep -c "paddingTop: 56" /Users/user/Zface/ustal/screens/SupportScreen.js && echo "FAIL: hardcoded paddingTop" || echo "OK"
cd /Users/user/Zface/ustal && npm test -- --passWithNoTests 2>&1 | tail -3
    </automated>
  </verify>
  <acceptance_criteria>
    - FishingScreen.js не содержит emoji символов '🌅', '☀️', '🌆', '🌙' как строки (все заменены на Ionicons names)
    - FishingScreen.js не рендерит result.emoji или c.emoji как Text
    - SupportScreen.js не содержит "paddingTop: 56" (заменено на insets.top + 12)
    - SupportScreen.js содержит "useSafeAreaInsets"
    - BreathingScreen.js содержит "useSafeAreaInsets" или "insets.top"
    - npm test зелёный
    Команды:
    ```bash
    grep -n "🌅\|☀️\|🌆\|🌙\|result\.emoji\|c\.emoji" /Users/user/Zface/ustal/screens/FishingScreen.js
    grep -n "paddingTop: 56" /Users/user/Zface/ustal/screens/SupportScreen.js
    grep -n "useSafeAreaInsets" /Users/user/Zface/ustal/screens/SupportScreen.js
    grep -n "useSafeAreaInsets\|insets.top" /Users/user/Zface/ustal/screens/BreathingScreen.js
    ```
    Все команды должны вернуть 0 строк (кроме useSafeAreaInsets — там ожидается > 0).
  </acceptance_criteria>
  <done>FishingScreen не содержит emoji в UI. SupportScreen и BreathingScreen используют корректные safe-area insets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| theme.js → все экраны | Токены дизайн-системы влияют на весь UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09-01 | Tampering | theme.js | accept | Изменение токенов влияет на весь UI — визуальное тестирование обязательно после применения. Риск регрессии UI минимальный (только цвет меняется). |
| T-07-09-02 | Information Disclosure | FishingScreen | accept | Удаление emoji из UI не влияет на безопасность. |
</threat_model>

<verification>
```bash
# Проверить theme.js
grep "accent:\|pink:" /Users/user/Zface/ustal/theme.js

# Проверить нет хардкода фиолетового
grep -rn "#7c3aed" /Users/user/Zface/ustal/ --include="*.js" | grep -v node_modules

# Проверить FishingScreen
grep -c "🌅\|☀️\|🌆\|🌙\|result\.emoji" /Users/user/Zface/ustal/screens/FishingScreen.js

# Проверить SupportScreen
grep "paddingTop" /Users/user/Zface/ustal/screens/SupportScreen.js | grep "56"

# Тесты
cd /Users/user/Zface/ustal && npm test -- --passWithNoTests
```

Визуальная проверка в Expo Go после изменений:
- Все кнопки должны быть тёплого коричневого (#8B7355), не фиолетового
- FishingScreen: result card показывает Ionicons рыбу вместо emoji
- SupportScreen/BreathingScreen: заголовок не обрезается нотчем
</verification>

<success_criteria>
- theme.js: accent=#8B7355, pink=#E07060
- Нет хардкода #7c3aed в JavaScript файлах проекта
- FishingScreen не рендерит emoji (ни result.emoji, ни scene emoji, ни c.emoji)
- SupportScreen использует insets.top вместо paddingTop: 56
- BreathingScreen использует insets.top
- npm test зелёный
</success_criteria>

<output>
После завершения создать .planning/phases/phase-07-release-prep/07-09-SUMMARY.md с:
- подтверждением изменения accent в theme.js
- списком файлов где убраны хардкоды #7c3aed
- подтверждением исправления FishingScreen, SupportScreen, BreathingScreen
</output>
