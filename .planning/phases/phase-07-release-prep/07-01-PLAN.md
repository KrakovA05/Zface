---
phase: 07-release-prep
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - ustal/app.json
autonomous: false
requirements:
  - RELEASE-01
must_haves:
  truths:
    - "app.json содержит финальное название приложения (не 'ustal')"
    - "app.json содержит slug, scheme, bundleIdentifier, package — все с финальным именем"
    - "expo-notifications color в app.json — #8B7355 (не #7c3aed)"
    - "ios.supportsTablet = false"
    - "splash.backgroundColor = #FAF7F2"
  artifacts:
    - path: "ustal/app.json"
      provides: "production-ready конфиг"
      contains: "bundleIdentifier"
  key_links:
    - from: "ustal/app.json"
      to: "ustal/eas.json (plan 07-02)"
      via: "android.package должен совпадать с Firebase App package name"
      pattern: "\"package\": \"com\\."
---

<objective>
Зафиксировать финальное название приложения и обновить app.json для production-сборки.

Purpose: app.json с именем 'ustal' и slug 'ustal' нельзя публиковать. bundleIdentifier, package name и scheme должны быть заданы до создания Firebase App (plan 07-02) — они должны совпасть.
Output: Обновлённый ustal/app.json с финальным именем, slug, scheme, bundleIdentifier, package, ios.supportsTablet=false, expo-notifications color=#8B7355.
</objective>

<execution_context>
@/Users/user/Zface/.planning/phases/phase-07-release-prep/07-RESEARCH.md
</execution_context>

<context>
@/Users/user/Zface/.planning/ROADMAP.md
@/Users/user/Zface/.planning/phases/phase-07-release-prep/CONTEXT.md

<interfaces>
Текущий ustal/app.json (полный файл — 38 строк):
```json
{
  "expo": {
    "name": "ustal",
    "slug": "ustal",
    "version": "1.0.0",
    "orientation": "portrait",
    "icon": "./assets/icon.png",
    "userInterfaceStyle": "light",
    "newArchEnabled": true,
    "splash": {
      "image": "./assets/splash-icon.png",
      "resizeMode": "contain",
      "backgroundColor": "#FAF7F2"
    },
    "plugins": [
      ["expo-notifications", { "icon": "./assets/icon.png", "color": "#7c3aed" }]
    ],
    "ios": {
      "supportsTablet": true
    },
    "android": {
      "adaptiveIcon": {
        "foregroundImage": "./assets/adaptive-icon.png",
        "backgroundColor": "#FAF7F2"
      },
      "edgeToEdgeEnabled": true
    },
    "web": {
      "favicon": "./assets/favicon.png"
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Подтвердить финальное название приложения</name>
  <decision>Финальное название приложения — «Ной»?</decision>
  <context>
    Кандидат: «Ной» (метафора: имя + «ноет»).
    От этого выбора зависит:
    - slug в app.json: noy
    - scheme (deep link): noy://
    - bundleIdentifier iOS: com.noy.app
    - package Android: com.noy.app
    - Firebase App package name (plan 07-02) — должен совпасть
    - GitHub Organization (plan 07-04)
    - ASO-метадата (plan 07-05)

    Если название другое — уточни его, и executor обновит все значения соответственно.
  </context>
  <options>
    <option id="noy">
      <name>«Ной»</name>
      <pros>Уже согласован, метафора работает, slug 'noy' короткий</pros>
      <cons>Нет</cons>
    </option>
    <option id="other">
      <name>Другое название</name>
      <pros>Если есть лучший вариант</pros>
      <cons>Нужно указать название и придумать slug (строчные латинские буквы)</cons>
    </option>
  </options>
  <resume-signal>Напиши «ной» или укажи другое название</resume-signal>
  <read_first>
    - /Users/user/Zface/.planning/phases/phase-07-release-prep/CONTEXT.md (Notes раздел — «Ной»)
  </read_first>
  <acceptance_criteria>
    Пользователь подтвердил название. Executor знает slug, scheme, bundleIdentifier, package.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Обновить app.json для production</name>
  <files>ustal/app.json</files>
  <read_first>
    - /Users/user/Zface/ustal/app.json (прочитать текущий файл целиком перед изменением)
    - /Users/user/Zface/.planning/phases/phase-07-release-prep/07-RESEARCH.md (Pattern 5: app.json production config)
  </read_first>
  <action>
    Перезаписать ustal/app.json с production-значениями. Если пользователь подтвердил «Ной» — использовать значения ниже. Если другое название — подставить соответствующий slug/scheme/bundleIdentifier/package.

    Итоговое состояние файла:
    - name: финальное название (если «Ной» → "Ной")
    - slug: slug в нижнем регистре (если «Ной» → "noy")
    - scheme: тот же slug (если «Ной» → "noy") — нужен для deep link plan 07-03
    - version: "1.0.0" — оставить
    - orientation: "portrait" — оставить
    - icon: "./assets/icon.png" — оставить
    - userInterfaceStyle: "light" — оставить
    - newArchEnabled: true — оставить
    - splash.image: "./assets/splash-icon.png" — оставить
    - splash.resizeMode: "contain" — оставить
    - splash.backgroundColor: "#FAF7F2" — уже верно, оставить
    - plugins expo-notifications color: изменить "#7c3aed" → "#8B7355" (тёплый коричневый дизайн-системы)
    - ios.supportsTablet: изменить true → false
    - ios.bundleIdentifier: добавить "com.noy.app" (или аналог для другого названия)
    - android.package: добавить "com.noy.app" (или аналог)
    - android.googleServicesFile: добавить "./google-services.json" — этот файл появится в plan 07-02
    - android.adaptiveIcon — оставить как есть
    - android.edgeToEdgeEnabled: true — оставить
    - web.favicon — оставить

    Секцию "web" не удалять.
  </action>
  <verify>
    <automated>node -e "const j=require('./ustal/app.json'); const e=j.expo; if(e.name==='ustal') throw 'name not updated'; if(!e.scheme) throw 'scheme missing'; if(!e.ios.bundleIdentifier) throw 'bundleIdentifier missing'; if(!e.android.package) throw 'package missing'; if(e.ios.supportsTablet!==false) throw 'supportsTablet must be false'; if(e.plugins[0][1].color!=='#8B7355') throw 'wrong notification color'; console.log('OK:', e.name, e.slug, e.scheme);" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - app.json.expo.name != "ustal"
    - app.json.expo.slug != "ustal"
    - app.json.expo.scheme присутствует и совпадает со slug
    - app.json.expo.ios.bundleIdentifier присутствует (формат: "com.*.app")
    - app.json.expo.android.package присутствует (тот же)
    - app.json.expo.android.googleServicesFile == "./google-services.json"
    - app.json.expo.ios.supportsTablet == false
    - app.json.expo.plugins[0][1].color == "#8B7355"
    - Команда node verify выше выводит "OK:" без ошибок
  </acceptance_criteria>
  <done>app.json обновлён для production — все Expo-заглушки убраны, production identifiers заданы.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app.json → EAS Build | Конфигурационный файл читается при сборке — должен содержать корректные идентификаторы |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Information Disclosure | app.json | accept | Файл публичный (в git) — содержит только публичные идентификаторы, никаких секретов |
| T-07-01-02 | Tampering | app.json | mitigate | Хранить в git с историей; все изменения отслеживаются через commits |
</threat_model>

<verification>
После выполнения обоих задач:
```bash
node -e "const j=require('./ustal/app.json'); console.log(JSON.stringify(j.expo, null, 2));" 2>&1
```
Проверить визуально: name, slug, scheme, ios.bundleIdentifier, android.package, ios.supportsTablet=false.
</verification>

<success_criteria>
- app.json не содержит "ustal" в name и slug
- scheme задан (нужен для plan 07-03)
- bundleIdentifier и package заданы (нужны для plan 07-02 Firebase App)
- ios.supportsTablet = false
- expo-notifications color = #8B7355
</success_criteria>

<output>
После завершения создать .planning/phases/phase-07-release-prep/07-01-SUMMARY.md с:
- финальным названием и slug
- bundleIdentifier / package
- всеми изменёнными полями app.json
</output>
