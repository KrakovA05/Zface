---
phase: 07-release-prep
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/report-alert/index.ts
autonomous: false
requirements:
  - RELEASE-06
user_setup:
  - service: supabase
    why: "RESEND_API_KEY нужен Edge Function для отправки email-алерта"
    env_vars:
      - name: RESEND_API_KEY
        source: "resend.com → API Keys → Create API Key (домен должен быть верифицирован)"
    dashboard_config:
      - task: "Добавить RESEND_API_KEY в Supabase Secrets"
        location: "Supabase Dashboard → Project Settings → Edge Functions → Secrets → Add new secret: RESEND_API_KEY"
      - task: "Создать Database Webhook"
        location: "Supabase Dashboard → Database → Webhooks → Create new webhook: Name=report-alert-webhook, Table=public.reports, Events=INSERT, Type=Supabase Edge Functions, Function=report-alert"
must_haves:
  truths:
    - "Edge Function report-alert существует и корректно отправляет email через Resend"
    - "Edge Function использует RESEND_API_KEY из переменных среды (не хардкод)"
    - "Email уходит на адрес разработчика при каждом новом INSERT в таблицу reports"
    - "Database Webhook настроен на INSERT в public.reports → Edge Function report-alert"
  artifacts:
    - path: "supabase/functions/report-alert/index.ts"
      provides: "Edge Function для email-алерта"
      contains: "RESEND_API_KEY"
  key_links:
    - from: "INSERT в public.reports"
      to: "Edge Function report-alert"
      via: "Database Webhook"
      pattern: "Deno.env.get.*RESEND"
    - from: "Edge Function"
      to: "Resend API"
      via: "fetch https://api.resend.com/emails"
      pattern: "resend.com/emails"
---

<objective>
Подключить email-алерт разработчику при каждой новой жалобе в таблице reports.

Purpose: Без уведомлений о жалобах безопасность сообщества не обеспечена — опасный контент может оставаться видимым часами. Алерт в реальном времени позволяет реагировать в течение минут.
Output: Рабочая Edge Function report-alert + Database Webhook на INSERT в reports. RESEND_API_KEY хранится в Supabase Secrets, не в коде.
</objective>

<execution_context>
@/Users/user/Zface/.planning/phases/phase-07-release-prep/07-RESEARCH.md
</execution_context>

<context>
@/Users/user/Zface/.planning/ROADMAP.md
@/Users/user/Zface/.planning/phases/phase-07-release-prep/07-RESEARCH.md

<interfaces>
Существующий паттерн Edge Function в проекте (из supabase/functions/compute-weekly-profile/index.ts):
```typescript
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';

Deno.serve(async (req) => {
  // обработка
  return new Response(JSON.stringify({ ok: true }), {
    headers: { 'Content-Type': 'application/json' },
  });
});
```

Webhook payload структура (из 07-RESEARCH.md):
```typescript
type WebhookPayload = {
  type: 'INSERT';
  table: 'reports';
  schema: 'public';
  record: {
    id: string;
    reporter_id: string;
    reported_user_id: string;
    reason: string;
    created_at: string;
  };
  old_record: null;
};
```

Resend API (уже используется в проекте через send-support-email):
```typescript
fetch('https://api.resend.com/emails', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${Deno.env.get('RESEND_API_KEY')}`,
  },
  body: JSON.stringify({ from, to, subject, html }),
})
```

ВАЖНО (Pitfall 5 из RESEARCH.md): При создании webhook в Supabase Dashboard выбирать тип
"Supabase Edge Functions" (не "HTTP Request") — авторизация настраивается автоматически.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Проверить и обновить Edge Function report-alert</name>
  <files>supabase/functions/report-alert/index.ts</files>
  <read_first>
    - /Users/user/Zface/supabase/functions/report-alert/index.ts (если существует — прочитать целиком)
    - /Users/user/Zface/.planning/phases/phase-07-release-prep/07-RESEARCH.md (Pattern 4: Database Webhook, строки 245-308)
    - /Users/user/Zface/supabase/functions/compute-weekly-profile/index.ts (строки 1-36, паттерн Edge Function)
  </read_first>
  <action>
    1. Проверить существует ли файл supabase/functions/report-alert/index.ts:
       - Если существует: прочитать и верифицировать что он использует Deno.env.get('RESEND_API_KEY') и отправляет на нужный email
       - Если не существует или некорректен: создать/переписать

    2. Итоговое содержимое supabase/functions/report-alert/index.ts:
    ```typescript
    // Email-алерт при новой жалобе пользователя
    // Вызывается через Database Webhook на INSERT в public.reports

    type WebhookPayload = {
      type: 'INSERT';
      table: 'reports';
      schema: 'public';
      record: {
        id: string;
        reporter_id: string;
        reported_user_id: string;
        reason: string;
        created_at: string;
      };
      old_record: null;
    };

    Deno.serve(async (req) => {
      try {
        const payload: WebhookPayload = await req.json();
        const { record } = payload;

        if (!record) {
          return new Response(JSON.stringify({ ok: false, error: 'no record' }), {
            headers: { 'Content-Type': 'application/json' },
            status: 400,
          });
        }

        const resendKey = Deno.env.get('RESEND_API_KEY');
        if (!resendKey) {
          console.error('RESEND_API_KEY not set');
          return new Response(JSON.stringify({ ok: false, error: 'no api key' }), {
            headers: { 'Content-Type': 'application/json' },
            status: 500,
          });
        }

        const res = await fetch('https://api.resend.com/emails', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
            'Authorization': `Bearer ${resendKey}`,
          },
          body: JSON.stringify({
            from: 'alerts@noy.app',
            to: ['krakov.arseniy@icloud.com'],
            subject: `[Zface] Новая жалоба: ${record.reason}`,
            html: `
              <h2>Новая жалоба</h2>
              <p><strong>ID записи:</strong> ${record.id}</p>
              <p><strong>Жалобщик (user_id):</strong> ${record.reporter_id}</p>
              <p><strong>На пользователя (user_id):</strong> ${record.reported_user_id}</p>
              <p><strong>Причина:</strong> ${record.reason}</p>
              <p><strong>Время:</strong> ${record.created_at}</p>
            `,
          }),
        });

        const data = await res.json();
        console.log('Resend response:', res.status, JSON.stringify(data));

        return new Response(JSON.stringify({ ok: res.ok, status: res.status }), {
          headers: { 'Content-Type': 'application/json' },
        });
      } catch (err) {
        console.error('report-alert error:', err);
        return new Response(JSON.stringify({ ok: false, error: String(err) }), {
          headers: { 'Content-Type': 'application/json' },
          status: 500,
        });
      }
    });
    ```

    ВАЖНО: email 'from' должен быть с верифицированного домена в Resend. Если домен noy.app не верифицирован — временно использовать 'onboarding@resend.dev' (Resend тестовый домен, работает без верификации).
    email 'to' — krakov.arseniy@icloud.com (личный, для уведомлений разработчика — это корректно, это не публичный email).

    3. После записи файла — задеплоить:
    ```bash
    cd /Users/user/Zface && npx supabase functions deploy report-alert
    ```
    (или использовать Supabase CLI если установлен)
  </action>
  <verify>
    <automated>test -f /Users/user/Zface/supabase/functions/report-alert/index.ts && grep -c "RESEND_API_KEY" /Users/user/Zface/supabase/functions/report-alert/index.ts && grep -c "resend.com/emails" /Users/user/Zface/supabase/functions/report-alert/index.ts && echo "OK" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - supabase/functions/report-alert/index.ts существует
    - Файл содержит "Deno.env.get('RESEND_API_KEY')" — ключ из переменных среды, не хардкод
    - Файл содержит "resend.com/emails" — правильный endpoint
    - Файл содержит "reporter_id" и "reported_user_id" в html теле письма
    - Файл НЕ содержит хардкода API ключа (grep на "sk_" или "re_" должен вернуть 0)
    Команды:
    ```bash
    grep -c "Deno.env.get.*RESEND_API_KEY" /Users/user/Zface/supabase/functions/report-alert/index.ts
    grep -c "resend.com/emails" /Users/user/Zface/supabase/functions/report-alert/index.ts
    grep "sk_\|re_live_\|re_test_" /Users/user/Zface/supabase/functions/report-alert/index.ts && echo "FAIL: api key hardcoded" || echo "OK: no hardcoded key"
    ```
  </acceptance_criteria>
  <done>Edge Function report-alert готова и задеплоена.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Добавить RESEND_API_KEY и создать Database Webhook</name>
  <read_first>
    - /Users/user/Zface/.planning/phases/phase-07-release-prep/07-RESEARCH.md (Pattern 4 Шаги 1-2, Pitfall 5)
  </read_first>
  <what-built>Edge Function report-alert обновлена и задеплоена. Осталось добавить секрет и создать webhook в Supabase Dashboard.</what-built>
  <how-to-verify>
    **Шаг 1 — RESEND_API_KEY в Supabase Secrets**
    1. Открой Supabase Dashboard → твой проект
    2. Слева: Project Settings → Edge Functions → Secrets
    3. Нажми "Add new secret"
    4. Name: RESEND_API_KEY
    5. Value: твой API ключ из resend.com (начинается с "re_")
    6. Сохрани

    Если ключа нет: зайди на resend.com → API Keys → Create API Key → скопируй.
    Если домен не верифицирован — в Edge Function от-поля используется 'onboarding@resend.dev' (тестовый домен Resend, работает без верификации).

    **Шаг 2 — Database Webhook**
    1. Supabase Dashboard → Database → Webhooks
    2. Нажми "Create new webhook"
    3. Заполни:
       - Name: report-alert-webhook
       - Table: public.reports
       - Events: только INSERT (галочка)
       - Type: Supabase Edge Functions (НЕ "HTTP Request")
       - Function: report-alert
    4. Сохрани

    **Проверка (создать тестовую жалобу):**
    В SQL Editor:
    ```sql
    INSERT INTO public.reports (reporter_id, reported_user_id, reason)
    VALUES (
      (SELECT user_id FROM users LIMIT 1),
      (SELECT user_id FROM users LIMIT 1 OFFSET 1),
      'test alert — можно удалить'
    );
    ```
    Через 10-30 секунд должен прийти email на krakov.arseniy@icloud.com.

    Удали тестовую запись после проверки:
    ```sql
    DELETE FROM public.reports WHERE reason = 'test alert — можно удалить';
    ```
  </how-to-verify>
  <resume-signal>Напиши "webhook готов" и укажи получил ли тестовый email</resume-signal>
  <acceptance_criteria>
    - RESEND_API_KEY добавлен в Supabase Secrets
    - Database Webhook создан: table=reports, event=INSERT, function=report-alert
    - Тестовая жалоба → email пришёл в течение 1 минуты
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database → Webhook → Edge Function | INSERT в reports → webhook payload → Edge Function |
| Edge Function → Resend API | RESEND_API_KEY используется только в runtime через Deno.env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Information Disclosure | RESEND_API_KEY | mitigate | Хранить только в Supabase Secrets (Deno.env), никогда не коммитить в код. Edge Function читает через Deno.env.get('RESEND_API_KEY'). |
| T-07-06-02 | Information Disclosure | email тела webhook | accept | Webhook payload содержит только UUID пользователей и причину жалобы — без PII (имён, email адресов). |
| T-07-06-03 | Denial of Service | массовые жалобы → flood email | accept | В MVP: низкий риск. Если проблема появится — добавить rate limit на уровне Edge Function (bucket per reporter_id). |
| T-07-06-04 | Spoofing | webhook без авторизации | mitigate | Использовать тип "Supabase Edge Functions" в Dashboard — Supabase автоматически добавляет Authorization header с service role key. |
</threat_model>

<verification>
```bash
# Проверить Edge Function
test -f /Users/user/Zface/supabase/functions/report-alert/index.ts
grep "RESEND_API_KEY" /Users/user/Zface/supabase/functions/report-alert/index.ts
grep "resend.com/emails" /Users/user/Zface/supabase/functions/report-alert/index.ts
```

Интеграционная проверка (после Task 2):
- INSERT в public.reports → email на krakov.arseniy@icloud.com в течение 60 секунд
</verification>

<success_criteria>
- supabase/functions/report-alert/index.ts использует Deno.env.get('RESEND_API_KEY')
- RESEND_API_KEY добавлен в Supabase Secrets
- Database Webhook активен (INSERT в reports → report-alert)
- Тестовая жалоба вызвала email в течение 1 минуты
</success_criteria>

<output>
После завершения создать .planning/phases/phase-07-release-prep/07-06-SUMMARY.md с:
- подтверждением деплоя Edge Function
- статусом тестовой проверки (email получен / не получен)
- ссылкой на webhook в Supabase Dashboard
</output>
